{
  "name": "trustpic-extension-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only tooling for TrustPic extension tests (not part of the packaged extension).",
  "scripts": {
    "test": "node test/analysis.test.mjs",
    "test:smoke": "node test/smoke.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^23.11.1"
  }
}
